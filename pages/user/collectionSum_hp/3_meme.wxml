<block wx:if="{{meme.length > 0}}">
    <view class="scrollbox">
        <!-- 加载一次的数据 -->
        <scroll-view bindscrolltolower="getMyCollectionHeadPortrait_MEME" class="scrollview" scroll-top="{{meme_scrollTop}}" enhanced="true" show-scrollbar="{{false}}" scroll-y="true" >
            <view wx:for="{{meme}}" wx:key="index">
                <!-- 广告 -->
                <view wx:if="{{meme_advertising_img[index].imgurl}}" style="height: 200rpx;width: 750rpx;float: left;overflow: hidden;">
                    <image catchtap="previewImage" data-url="{{meme_advertising_img[index].imgurl}}" src="{{meme_advertising_img[index].imgurl}}" style="height: 200rpx;width: 750rpx;position: relative;" mode="scaleToFill"></image>
                </view>
                <!-- 头像框 -->
                <view wx:for="{{item.array}}" wx:key="index" class="imgbox">
                    <image class="img" src="{{item.thumbnail}}" catchtap="goDetails" data-data="{{item}}"></image>
                </view>
            </view>
            <!-- mp-loading加载提示 -->
            <view style="width: 750rpx; height: 100rpx;float: left;" wx:if="{{meme_loading}}">
                <mp-loading style="position: relative;top: 40rpx;"></mp-loading>
            </view>
            <!-- 全部加载完成提示 -->
            <view class="end" wx:if="{{meme_end}}">-END-</view>
        </scroll-view>
    </view>
</block>
<block wx:else>
    <mp-loading class="mp-loading" />
</block>